document.getElementById('locate')?.addEventListener('click',()=>{const status=document.getElementById('location-status');if(!document.getElementById('location-consent').checked){status.textContent='Activa el permiso opcional primero.';return;}if(!navigator.geolocation){status.textContent='Tu navegador no ofrece ubicación; usaremos el municipio.';return;}navigator.geolocation.getCurrentPosition(p=>{document.getElementById('lat').value=p.coords.latitude;document.getElementById('lng').value=p.coords.longitude;status.textContent='Ubicación lista para guardar.';},()=>{status.textContent='No se compartió ubicación; usaremos el municipio.';},{timeout:8000});});
