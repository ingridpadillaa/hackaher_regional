rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // React calls authenticated Functions. Authorization is checked there before
    // every Admin SDK operation. No client can create membership or write finance data.
    match /{document=**} { allow read, write: if false; }
  }
}
